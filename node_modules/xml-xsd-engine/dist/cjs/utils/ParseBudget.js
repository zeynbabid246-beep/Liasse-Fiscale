"use strict";
/**
 * ParseBudget — Phase 13: Parsing cost tracking + timeout
 * ─────────────────────────────────────────────────────────
 * Provides a configurable "budget" that can abort parsing when any of
 * these limits is exceeded:
 *
 *   • wallTime   – maximum elapsed milliseconds
 *   • bytes      – maximum input bytes processed
 *   • nodes      – maximum DOM nodes created
 *   • depth      – maximum nesting depth  (also in ParseOptions)
 *
 * Usage
 * ─────
 *  const budget = new ParseBudget({ wallTimeMs: 2000, nodes: 500_000 });
 *  const doc = budget.parse(hugeXml);    // throws XmlError(SEC_*) if exceeded
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ParseBudget = void 0;
const XmlParser_1 = require("../parser/XmlParser");
const XmlError_1 = require("../errors/XmlError");
class ParseBudget {
    constructor(opts = {}) {
        this.opts = opts;
    }
    /**
     * Parse `input` under the configured budget.
     * Throws XmlError with code SEC_* if any limit is exceeded.
     */
    parse(input) {
        // Byte check (pre-parse, O(1))
        const byteLen = Buffer.byteLength(input, 'utf8');
        if (this.opts.bytes !== undefined && byteLen > this.opts.bytes) {
            throw (0, XmlError_1.secError)('SEC_MAX_TEXT_LENGTH', `Input exceeds byte limit (${byteLen} > ${this.opts.bytes} bytes)`);
        }
        const start = Date.now();
        const options = {
            ...(this.opts.parseOptions ?? {}),
            maxDepth: this.opts.depth ?? this.opts.parseOptions?.maxDepth ?? 500,
            maxNodeCount: this.opts.nodes ?? this.opts.parseOptions?.maxNodeCount ?? 1000000,
            maxAttributes: this.opts.maxAttributes ?? this.opts.parseOptions?.maxAttributes ?? 256,
            maxTextLength: this.opts.maxTextLength ?? this.opts.parseOptions?.maxTextLength ?? 10000000,
        };
        const doc = (0, XmlParser_1.parseXml)(input, options);
        // Wall-time check (post-parse — the parser is synchronous so we check after)
        if (this.opts.wallTimeMs !== undefined) {
            const elapsed = Date.now() - start;
            if (elapsed > this.opts.wallTimeMs) {
                throw (0, XmlError_1.secError)('SEC_MAX_TEXT_LENGTH', `Parsing exceeded time limit (${elapsed}ms > ${this.opts.wallTimeMs}ms)`);
            }
        }
        return doc;
    }
    /**
     * Run a cost report for the given input without throwing.
     * Useful for profiling / monitoring.
     */
    report(input) {
        const violations = [];
        const bytes = Buffer.byteLength(input, 'utf8');
        if (this.opts.bytes !== undefined && bytes > this.opts.bytes) {
            violations.push(`byte limit exceeded (${bytes} > ${this.opts.bytes})`);
        }
        const start = Date.now();
        try {
            const options = {
                maxDepth: this.opts.depth ?? 500,
                maxNodeCount: this.opts.nodes ?? 1000000,
                maxAttributes: this.opts.maxAttributes ?? 256,
                maxTextLength: this.opts.maxTextLength ?? 10000000,
            };
            (0, XmlParser_1.parseXml)(input, options);
        }
        catch (e) {
            violations.push(e.message);
        }
        const elapsedMs = Date.now() - start;
        if (this.opts.wallTimeMs !== undefined && elapsedMs > this.opts.wallTimeMs) {
            violations.push(`time limit exceeded (${elapsedMs}ms > ${this.opts.wallTimeMs}ms)`);
        }
        return { bytes, elapsedMs, withinBudget: violations.length === 0, violations };
    }
}
exports.ParseBudget = ParseBudget;
//# sourceMappingURL=ParseBudget.js.map